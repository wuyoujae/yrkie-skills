# 图片上传与 Outline 确认（0.5.0，2026-09-07）

完整设计见主项目 `docs/AGENT_PLUGIN_IMAGES_CONFIRM_PLAN.md`。用户已明确沿用 10/30/100 页套餐限制；实现与隔离联调已完成；正式服务需先迁移至 v52，插件更新后重新授权。

- [x] 核对生成图片、素材引用、存储、确认锁、失败重试和计费代码。
- [x] 明确独立 Schema/工作流目录、上传/确认配套接口与用户同意关口。
- [x] 拆分 Skill 参考文件，同步机器 Schema、混合图片示例和错误指导。
- [x] 实现外部图片上传、受控素材绑定及后续素材清单。
- [x] 实现确认预检查、原子确认、图片状态及显式收费重试。
- [x] 完成迁移/权限、MCP/浏览器、图像安全与恢复验收后记录交付状态。

# 0.4.0 DOE 项目与 Outline 创建

用户已授权完成两个创建工具、完整 Outline Schema 和严格校验；确认与 Slide 写入后续另行开发。方案见主项目 docs/AGENT_PLUGIN_OUTLINE_WRITE_PLAN.md。

- [x] 新建 DOE 项目、Outline 新版本、幂等 UUID 与版本/修订检查。
- [x] 完整作者文档、JSON Schema、三个示例、诊断与重试指导。
- [x] Core/最小权限 PG/Web/stdio/前端权限联调。
- [x] 本地迁移、文档与 0.4.0 发布准备（生产部署单独执行）。

以下为历史记录。

# Yrkie 插件：账号绑定与项目读取

## 0.3.0 DOE 单页 Markdown 读取（用户已确认实施）

方向：新增 `yrkie_project_slide`，由平台按完整项目合同分派读取器；目前仅 DOE。复用账号/授权绑定的临时项目引用，新增明确批准的 slides:read 范围，单页带稿件 revision。

- [x] 新工具与客户端输入/输出白名单，旧 scope 兼容。
- [x] Skill 单页/多页读取、版本变化、格式不支持及重新授权说明。
- [x] 平台组件转换、真实 PostgreSQL 隔离、stdio 与浏览器联调全部通过。
- [x] 版本更新到 0.3.0，文档同步；本地平台 v50 已运行，生产部署独立进行。

状态：0.3.0 已完成 DOE 单页读取及本地联调，生产部署仍需单独执行。

## 本次范围

用户在支持 stdio MCP 的 Agent 安装插件，在 Yrkie 网站登录并确认授权，然后查询本人项目数量、项目信息、Markdown 大纲和 DOE 单页内容。项目范围与平台 Library 一致；不开放其他账号查询。

## 实现方向

- TypeScript + 官方 MCP SDK，使用本地 stdio 传输；Skill 教会 Agent 何时绑定、查询和处理失效授权。
- 通过设备授权流程在官网确认，新申请包含项目和大纲读取权限，旧 count-only 授权不自动扩权；密码、网站 Cookie 和访问凭据不进入模型上下文。
- 平台提供独立的授权与计数 API；开源插件不连接数据库、不承载计费规则。
- 同一套 MCP 和 Skill，分别提供 Codex、Claude Code 所需的插件清单与安装说明。
- 6 美元订阅档位、Schema 创作、PPT 编辑保存、渲染及导出不在本次实现范围。

## 工作清单

- [x] 通用 MCP 配置生成器，客户端快捷安装支持显式本地 origin。
- [x] Skill 携带现有组件 Schema，明确所有平台通信仅通过 MCP。
- [x] 备份并迁移本地数据库，启用与重启现有网站，保留可回退版本。
- [x] 验证本地服务、MCP 配置与 Skill 合同，交付其他 Agent 可用的配置。

- [x] 建立独立 Git 仓库，关联 `git@github.com:wuyoujae/yrkie-skills.git`。
- [x] 核对现有网站认证、项目列表及插件协议约束。
- [x] 确认平台授权、数据库和 API 的实施计划。
- [x] 实现平台设备授权、凭据撤销和最小权限计数接口。
- [x] 实现官网绑定确认与已授权设备撤销入口。
- [x] 实现 MCP 绑定开始、绑定完成、状态、计数和解绑工具。
- [x] 实现操作系统安全凭据存储；凭据不写入源码、插件配置或工具返回。
- [x] 编写 Skill、两种客户端插件清单、安装与开源许可证文件。
- [x] 验证真实授权、跨账号隔离、过期/撤销、并发兑换、项目计数一致性。
- [x] Codex 与 Claude Code 隔离配置安装、MCP 工具发现；真实浏览器与 stdio 客户端完成绑定、计数、状态、解绑。
- [ ] 发布前补充两种模型对话、市场安装及 macOS/Linux 凭据库验收。
- [ ] 完成公开仓库内容审查、版本发布及平台部署后再标记可用。

## 验收

未绑定时明确提示绑定；用户在官网批准后返回真实数量，空账号返回 0；未授权、拒绝、过期、撤销或平台错误不能伪装为 0。只读凭据不能访问编辑、导出或其他用户数据。两种客户端使用同一账号得到相同结果。

## 0.1.1 链接授权

- [x] 优先展示短期完整授权链接，无需手填码；旧服务兼容短码。
- [x] 严格校验链接 origin、路径和令牌格式，设备密钥不进入链接。
- [x] 更新 MCP/Skill 工作流；18 项插件测试通过。

## 授权页不再回退输入码

- [x] 缺少请求时提示重新发起，不显示输入框或可提交的授权按钮。
- [x] MCP 必须返回完整授权链接，不接受旧响应静默降级。
- [x] 验证页面状态、插件测试并同步发布。

## 0.1.3 按应用授权

- [x] 自动取得 MCP 客户端名称并申请一次性链接。
- [x] 按应用隔离凭据，网页读取服务端请求后显示具体应用。
- [x] 本地 v47 迁移、单次授权与真实 MCP 名称传递验证。

## 0.2.0 项目和大纲读取

- [x] 三个工具：项目名称检索、概况、Markdown 大纲。
- [x] 服务端映射 4 小时临时 projectRef，账号与授权双重隔离；模型不接收内部 ID。
- [x] 浏览器明确同意读取内容；旧授权需重新绑定，不能静默扩权。
- [x] Skill 仅描述 MCP 工作流、过期处理和不可信内容边界；不暴露 HTTP 合同。
- [x] MCP 真实 stdio 协议、参数/输出校验、失败脱敏及完整 Schema 引用测试。
- [x] 完成本地浏览器到 MCP 的读取验收：Codex/Claude Code 协议身份、最小权限 Web、项目概况、Markdown 大纲及解绑。
- 版本：0.2.0；生产服务仍需单独部署。
