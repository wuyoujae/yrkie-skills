{
  "name": "yrkie-agent-plugin",
  "version": "0.4.0",
  "description": "Read Yrkie projects, outlines and DOE slides; create DOE projects and save agent-authored outline versions.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wuyoujae/yrkie-skills.git"
  },
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "@napi-rs/keyring": "2.0.0",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "@types/node": "22.19.15"
  }
}
